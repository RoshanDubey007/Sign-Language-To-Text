So here is the thing, we use a sampling system where all this alphabets will be saved and sampled
	1. Image Sampler from camera.
	2. Teachable Machine where we can adjust  Classes(unique thing to identify)
	3. Save the model and make it work with a perfect UI which can see and report the required Output.